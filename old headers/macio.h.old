#ifndef __MACIO_H#define __MACIO_H#include <files.h>#include "mstdio.h"typedef struct{	short fd;		/*mac file reference number*/	short vrefnum; 	/*mac directory file is in*/	char  name[64]; /*Name of file*/	short rfd;		/*resource parts fd*/} MFILE;extern short current_vrefnum;	/*last vrefnum we used*/#ifdef __MACIO_C#define  DLGLEFT 	85#define  DLGTOP		100short current_vrefnum;	/*last vrefnum we used*/#endifMFILE *mopen(	char *ftype	/* types of files to display*/);MFILE *mopen_na(	char *name,		short vrefnum);MFILE *mcreat(	char *prompt,	/* prompt string*/	char *filename,	/*filename */	OSType creator,	OSType type);#define munlink fsdeletelong mwrite(	short fd,	char *buff,	long count);long mread(	short fd,	char *buff,	long count);long mclose(	MFILE *fp);long mlseek(	short fd,	int 	offset,	short	whence);long mlgetpos(	short fd);void settype(	char *filename,	short vRefNum,	char *type,	char *creator);short get_vRefNum(	char * buff );#endif