#include	"mstdio.h"char *mfgets(	char *s,	long n,	register M_FILE *iop){	register c;	register char *cs;	cs = s;	while (--n>0 && (c = mgetc(iop))>=0) {		*cs++ = c;		if (c=='\n')			break;	}	if (c<0 && cs==s)		return(NULL);	*cs++ = '\0';	return(s);}