#include	"mstdio.h"#include	<Unix_errno.h>M_FILE *mfreopen(	char *file,	short vrefnum,	char *mode,	M_FILE *iop){//	extern M_FILE *_mdoopen() ;	if (iop == NULL) {		/* 002 invalid arguement - set errno */		errno = EINVAL;		return (NULL);	}	(void)mfclose(iop);	return(_mdoopen(file,vrefnum,mode,iop));}