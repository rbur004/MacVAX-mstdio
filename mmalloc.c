#include <Stdlib.h>#include <memory.h>#include "mmalloc.h"#ifndef min#define min(a,b)	(((a) < (b)) ? (a):(b))#endifvoid *mmalloc(unsigned int size){	return NewPtr((Size) size);}void *mcalloc(unsigned int nelements, unsigned int element_size){	return NewPtrClear((Size) (nelements * element_size));}void mfree(void *p){	DisposPtr(p);}void *mrealloc(void *ptr, unsigned int size){char *new_block;//int i;//char *p, *q;	if(new_block = NewPtr((Size) size)) //Yes 1 ='s	{		BlockMove(ptr, new_block, (Size) min(size, GetPtrSize(ptr)) );//		p = new_block; q = ptr;//		for(i = 0; i < min(size, GetPtrSize(ptr)); i++)//			*p++ = *q++;					return new_block;	}	return (void *) 0;}